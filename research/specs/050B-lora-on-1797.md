# Spec 050B — Per-pass FFN LoRA on 1797 baseline

**Date:** 2026-04-27
**Branch:** `exp/050B-lora-on-1797` @ `787f672`
**Launch script:** `tmp_exec/launch_050B_lora.sh`

## Hypothesis

Activation-side rank-2 LoRA on MLP up/down projections, stacked on 050A AC-fix levers. Pre-materializes `A@B` delta outside compile via `@dynamo.disable` to avoid narrow-K Triton hang. Each pass applies a distinct learned delta without duplicating weight storage.

## Baseline

Spec 050 (`f5b8af8`) — pre-quant EMA val_bpb ~1.067. Also compare to 050A.

## Expected Δ

−0.001 to −0.003 pre-quant vs 050 baseline. Uncertain.

## Accept criteria

- Pre-quant EMA val_bpb ≤ 1.067.
- Smoke log shows `layer_loop:enabled` and `loop_rewarm` — no hang at loop activation.

## Config diff (vs spec 050)

```
LOOP_SCALE_INIT=recip
LOOP_ITER_EMBEDS=1
LOOP_FFN_LORA_RANK=2
```

## Code changes

Branch: `exp/050B-lora-on-1797` @ `787f672`
Train script: `records/track_10min_16mb/2026-04-27_050_PR1797_Base_BOS_Fix/train_gpt.py`

- `Block.forward`: optional `lora_delta_up`, `lora_delta_down` kwargs applied in MLP path
- `GPT.__init__`: `loop_ffn_up/down_lora_A/B` params `[passes, loop_layers, rank, dim]`; kaiming-A/zero-B
- `_compute_lora_deltas()`: eager A@B materialization (no decorator — called only from outside compile)
- `_forward_hidden`: accepts `lora_deltas=None` kwarg; caller pre-folds A@B in eager and passes it in. `forward()` / `forward_logits()` thread it through. Training step (`step_fn`), `_run_cu_bucket_warmup`, `eval_val`, and GPTQ `collect_hessians` all pre-compute before the compiled call. Required because `@torch._dynamo.disable` is unsupported under `fullgraph=True` (smoke crashed on b6305c8 with `Skip inlining` error).

## Hardware ladder

- **4×H100 mini** — 10-min smoke then 20-min screen (smoke required: new None→tensor graph variant).
- **8×H100 official** — if mini shows clear improvement.

## Seed plan

Single seed (42).

## Inputs

`/workspace/parameter-golf/data/datasets/fineweb10B_sp8192_caseops/...` No hotstart.

## Checkpoints

`final_model.pt` from GPTQ.

## Stop-early criteria

NaN, train_loss > 7.0 after step 200, step time > 1.5× baseline at loop activation.

## Cost estimate

~$3 (10-min smoke + 20-min screen, 4×H100).

## Open questions for interview

Verify smoke log shows `layer_loop:enabled` and `loop_rewarm`. Watch step time spike at loop activation.
