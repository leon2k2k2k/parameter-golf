# Spec 050B — Per-pass FFN LoRA on 1797 baseline

**Date:** 2026-04-27
**Branch:** `exp/050B-lora-on-1797` @ `a680014`
**Launch script:** `tmp_exec/launch_050B_lora.sh`

## Hypothesis

Activation-side rank-2 LoRA on MLP up/down projections, stacked on 050A AC-fix levers. Pre-materializes `A@B` delta outside compile to avoid narrow-K Triton hang. Each pass applies a distinct learned delta without duplicating weight storage.

## Baseline

Spec 050 (`f5b8af8`) — pre-quant EMA val_bpb ~1.067. Also compare to 050A.

## Expected Δ

−0.001 to −0.003 pre-quant vs 050 baseline. Uncertain.

## Accept criteria

- Pre-quant EMA val_bpb ≤ 1.067.
- Smoke log shows `layer_loop:enabled` and `loop_rewarm` — no hang at loop activation.

## Config diff (vs spec 050)

```
LOOP_SCALE_INIT=recip
LOOP_ITER_EMBEDS=1
LOOP_FFN_LORA_RANK=2
```

## Code changes

Branch: `exp/050B-lora-on-1797` @ `a680014`
Train script: `records/track_10min_16mb/2026-04-27_050_PR1797_Base_BOS_Fix/train_gpt.py`

- `Block.forward`: `lora_delta_up`, `lora_delta_down` kwargs applied in MLP path
- `GPT.__init__`: `loop_ffn_up/down_lora_A/B` params `[passes, loop_layers, rank, dim]`; kaiming-A/zero-B; registers `_lora_delta_up/down_id` zero buffers for non-loop-layer steps
- `_compute_lora_deltas()`: eager A@B materialization, always called when `loop_ffn_lora_rank>0` (no `looping_active` guard)
- `_forward_hidden` enc/dec loops: always pass real tensors — learned delta for loop-layer steps, identity zeros for non-loop steps. Single `Block.forward` compiled graph variant, no None→Tensor switch at loop activation.
- All 4 call sites (`step_fn`, `_run_cu_bucket_warmup`, `eval_val`, `collect_hessians`): always compute deltas when rank>0.

## Hardware ladder

- **4×H100 mini** — 200s sanity smoke (ENABLE_LOOPING_AT=0.05, loop fires ~10s) then 20-min screen. Smoke verifies post-rewarm training proceeds — prior versions hung after loop_rewarm with GPUs at 100% for 7+ min. Script aborts if no training steps appear after loop activation.
- **8×H100 official** — if mini shows clear improvement.

## Seed plan

Single seed (42).

## Inputs

`/workspace/parameter-golf/data/datasets/fineweb10B_sp8192_caseops/...` No hotstart.

## Checkpoints

`final_model.pt` from GPTQ.

## Stop-early criteria

NaN, train_loss > 7.0 after step 200, step time > 1.5× baseline at loop activation.

## Cost estimate

~$2.50 (200s smoke + 20-min screen, 4×H100).

## Open questions for interview

Smoke must show training steps AFTER loop_rewarm (prior hang showed no steps post-rewarm). Script auto-aborts if this check fails.
