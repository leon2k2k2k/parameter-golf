# Idea 035 — `MIN_LR` on the original `030` alpha/beta family

## Thesis

The strongest schedule signal so far is:

- `034cB` improved a weaker frozen-carry branch materially

The obvious next transfer test is:

- same `MIN_LR=0.10`
- on the stronger original `030` alpha/beta family
- first as a `4×H` pre-quant screen

## Benchmark

Best relevant `4×H` alpha/beta-family reference:

- `026` screen seed `314`: pre-quant `1.06770372`

## First question

Can `MIN_LR=0.10` beat `1.06770372` on the original `030` family in `4×H`
screen form?
