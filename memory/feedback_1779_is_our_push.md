`#1779` is our own push / internal line, not an external public baseline to reason about as "theirs".

Use it as:
- our frozen recurrent `alpha/beta` line
- something `#1787` may stack on top of

Do not talk about `#1779` as if it were a separate outside result family.
